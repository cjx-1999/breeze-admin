export enum pTypes {
    echarts = 'echarts',
    isDark = 'isDark',
    proxy = 'proxy',
    router = 'router',
    route = 'route',
    _ = '_',
}

export let varType:
    '--loading' | '--dark-bg' | '--light-bg'



